const somaCalculadora = (a, b) => a + b

const subtracaoCalculadora = (a, b) => a - b

const multiplicacaoCalculadora = (a, b) => a * b

const divisaoCalculadora = (a, b) =>  a / b

export { somaCalculadora, 
         subtracaoCalculadora,
         multiplicacaoCalculadora,
         divisaoCalculadora }